# Web environment variables
# Copy to .env.local and fill in values. NEVER commit .env.local!

# ── Client-side (embedded in bundle — safe/public values only) ──
VITE_CHAIN_ID=137
VITE_USDC_ADDRESS=0x3c499c542cEF5E3811e1192ce70d8cC03d5c3359
VITE_FACTORY_ADDRESS=
VITE_REPUTATION_REGISTRY_ADDRESS=
VITE_POLYGON_RPC_URL=https://polygon.drpc.org
VITE_WALLETCONNECT_PROJECT_ID=

# ── Server-side only (Vercel serverless functions — NOT exposed to client) ──
# Set these in Vercel dashboard, NOT with VITE_ prefix
PINATA_JWT=
PINATA_GATEWAY_URL=https://gateway.pinata.cloud
